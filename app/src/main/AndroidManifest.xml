<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="www.samnang_alex.com.applicationaccesathletique">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".views.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.CreerUneBlessureActivity"
            android:label="@string/title_activity_creer_une_blessure"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".views.GestionDesEcolesActivity" />
        <activity
            android:name=".views.GenererRapportTherapeuteActivity"
            android:label="@string/title_activity_generer_rapport_therapeute"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>